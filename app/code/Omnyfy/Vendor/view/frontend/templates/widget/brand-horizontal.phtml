<?php
/**
 * Copyright © Omnyfy, Inc. All rights reserved.
 * See COPYING.txt for license details.
 *
 * Author: Kateryna Bieliaieva
 * Date: 17/8/2020
 */


/** @var \Omnyfy\Vendor\Block\Widget\ProductVendor $block */

$vendor = $block->getVendor();

if ($vendor):
    $logo = $vendor->getLogo();
    $label_text = $block->escapeHtml($block->getData('label_text'));
    $buttonText = $block->escapeHtml($block->getData('view_button_text'));
    ?>
<div class="product-vendor vendor horizontal">
    <a href="<?= $block->getVendorUrl($vendor);?>" title="<?= $vendor->getName();?>">
        <div class="vendor-details">
            <?php if (!empty($logo)): ?>
                <img class="vendor-logo" src="<?php echo $block->getLogoUrl($vendor);?>" alt="<?php echo $vendor->getName();?>"/>
            <?php endif; ?>

            <div>
                <div class="vendor-title">
                    <?= $label_text ? $label_text : __('Sold by') ?>
                </div>
                <div class="vendor-name">
                    <?= $vendor->getName() ?>
                </div>
            </div>
        </div>
        <span class="vendor-button"><?= $buttonText ? $buttonText : __('View profile') ?></span>
    </a>
</div>

<?php endif; ?>